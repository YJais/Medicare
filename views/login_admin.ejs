<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LogIn-Medicare</title>
    <link rel="stylesheet" href="/css/sgn&lgn.css">
    <script type="text/javaScript">
       <% if (typeof success !== 'undefined') { %>
            alert("<%= success %>");
        <% } %>
    </script>
</head>
<body>
    <div>
        <%-include('title')%>
        <hr>
    </div>
    <br>
    <div>
        <table width="300px" height="500px" align="center" id="loginform">
            <tr>
                <td>
                    <form action="/login_adminbtn" method="post">  <!--To Do implement action method-->
                        
                        <fieldset>
                            <h1 class="frmtitle"><strong>Log In</strong></h1>
                            <label>Username</label>
                            <input type="text" name="adname" id="adname" required>
                            <% if (errors && errors.ad_name) { %>
                                <p style="color: red;"><%= errors.ad_name %></p>
                            <% } %>
                            <label>Password</label>
                            <input type="password" name="adpass" id="adpass" required>
                            <% if (errors && errors.adpassword) { %>
                                <p style="color: red;"><%= errors.adpassword %></p>
                            <% } %>
                            <input type="submit" value="Log In" name="lgnbtn" id="lgnbtn">
                        </fieldset>
                    </form> 
                </td>
            </tr>
        </table>
       
    </div>
</body>
</html>